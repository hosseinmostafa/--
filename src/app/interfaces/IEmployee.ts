export interface IEmployee {
    id: string;
    name: string;
    position: string;
    department: string;
    age: number;
    salary: number;
    startDate: string; // You can also use Date type if handling Date objects
    email: string;
}
