import { LaunchDatePipe } from './launch-date.pipe';

describe('LaunchDatePipe', () => {
  it('create an instance', () => {
    const pipe = new LaunchDatePipe();
    expect(pipe).toBeTruthy();
  });
});
